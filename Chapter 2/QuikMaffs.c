/*Mans not hot*/
#include <stdio.h>

int main(void)
{
	int x;
	printf("Enter a number for some quik maffs: ");
	scanf("%d", &x);
	printf("BAM, SKrrrr-POP POP: %d\n", (3 * (x * x * x * x * x)) + (2 * (x * x * x* x)) - (5 * (x * x * x)) - (x * x) + (7 * x) - 6);
	
	return 0;
}

